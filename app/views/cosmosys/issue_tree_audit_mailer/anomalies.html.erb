<p><%= l(:mail_body_cosmosys_issue_tree_audit,
         roots: @summary.fetch(:checked_roots),
         items: @summary.fetch(:checked_items),
         inconsistent: @summary.fetch(:inconsistent_roots)) %></p>

<ul>
  <% @lines.each do |line| %>
    <li><%= line.delete_prefix('- ') %></li>
  <% end %>
</ul>

<p><%= l(:mail_body_cosmosys_issue_tree_audit_repair_warning) %></p>
